name: Messaging framework

on:
  push:
    branches:
      - gateway-messaging-framework-v3
    paths:
      - "messaging/**"
      - "skills/road-messaging-fabric/**"
      - "MESSAGING-FRAMEWORK.md"
      - ".github/workflows/messaging-framework.yml"
  pull_request:
    paths:
      - "messaging/**"
      - "skills/road-messaging-fabric/**"
      - "MESSAGING-FRAMEWORK.md"
      - ".github/workflows/messaging-framework.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Verify messaging contracts, MCP tools, and tests
        working-directory: messaging
        run: npm run verify
